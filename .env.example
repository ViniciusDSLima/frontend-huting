# Base URL da API (sem barra final). Em dev com proxy do Vite, use caminho relativo:
VITE_API_BASE_URL=/api/v1

# Alvo do proxy em desenvolvimento (vite serve → backend)
VITE_DEV_PROXY_TARGET=http://localhost:8080
